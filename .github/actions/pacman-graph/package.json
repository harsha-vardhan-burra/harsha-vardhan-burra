{
  "name": "pacman-contribution-graph",
  "version": "1.0.0",
  "description": "A GitHub Action that renders your contribution graph as an animated SVG with Pac-Man eating your contribution days. No ghosts, no pellets, no game logic \u2014 just a graph with a moving muncher.",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "node --test dist/test/*.test.js"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.2",
    "@types/node": "^20.14.0"
  }
}
